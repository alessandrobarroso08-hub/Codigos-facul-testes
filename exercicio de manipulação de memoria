#include <iostream>
#include <vector>

using namespace std;


int main() {
    
    int tamanho, x = 0;
    
    cout<<"digita o tamanho do vetor ae! " << endl;
    cout<<"-> ";
    cin>>tamanho;
    
    
    int *vec = new int[tamanho];
    int *v = new int[tamanho];
    
    cout<<" agora digita os numero do vetor " << endl;
    
    for(int i = 0; i < tamanho; i++){
        cout<<"-> ";
        cin>> vec[i];
            if(vec[i] % 2 == 0){
                v[x] = vec[i];
                x += 1;
            }
            else{
                x = x;
            }
    }
    
    
        
    for(int i = 0; i < tamanho; i++){
        cout<< vec[i] << " ";
    }
        cout << endl;
        cout<< "quantidade de numeros pares: " << x;
        cout<< endl;
        
   for(int p = 0; p < x; p++){
       cout<< v[p] << " ";
   } 
    
    delete[] vec;
    delete[] v;
    vec  = nullptr;

    return 0;
}
